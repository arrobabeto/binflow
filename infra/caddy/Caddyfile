{$BINFLOW_DOMAIN} {
	encode zstd gzip

	@auth path /api/auth/*
	handle @auth {
		reverse_proxy dashboard:3000
	}

	@api path /api/v1/* /webhooks/*
	handle @api {
		reverse_proxy api:8080
	}

	handle {
		reverse_proxy dashboard:3000
	}
}
