{
  "name": "@binflow/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx watch src/server.ts",
    "lint": "eslint src --max-warnings=0",
    "start": "node dist/server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@binflow/contracts": "workspace:*",
    "@binflow/domain": "workspace:*",
    "fastify": "5.11.3"
  },
  "devDependencies": {
    "@types/node": "24.13.3"
  }
}
